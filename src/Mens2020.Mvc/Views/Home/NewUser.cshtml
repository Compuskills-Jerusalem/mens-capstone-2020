@model IEnumerable<Mens2020.DataSource.Models.UserEvent>
@{
    ViewBag.Title = "NewUser";
}


@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

    }
    //todo: Redirect New user to create a goal
    @Html.ActionLink("Hi " + Capstone2020UserExtensions.GetFirstName(User.Identity) + ", let's get started!", "CreateQuick", routeValues: null, htmlAttributes: new { title = "CreateQuick" })

}